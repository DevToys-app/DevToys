namespace DevToys.Tools.Tools.Converters.Date;

internal enum DateConverterGridColumn
{
    Content
}

internal enum DateConverterGridRow
{
    Header,
    Content,
    Footer
}

internal enum DateConverterGridDateRow
{
    Content
}

internal enum DateConverterGridDateColumn
{
    Year,
    Month,
    Day
}

internal enum DateConverterGridTimeColumn
{
    Hour,
    Minutes,
    Seconds,
    Milliseconds
}

internal enum DateConvertDstGridRow
{
    Daylight,
    Offset,
    DateTime
}

internal enum DateConvertDstGridColumn
{
    LeftTitle,
    LeftContent,
    RightTitle,
    RightContent
}
