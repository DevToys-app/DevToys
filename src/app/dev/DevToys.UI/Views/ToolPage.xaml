<Page
    x:Class="DevToys.UI.Views.ToolPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="using:DevToys.UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:collections="using:CommunityToolkit.Mvvm.Collections"
    xmlns:converters="using:DevToys.UI.Framework.Converters"
    xmlns:ix="using:Microsoft.Xaml.Interactions.Core"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:tools="using:DevToys.Core.Tools"
    xmlns:strings="using:DevToys.Localization.Strings.MainWindow"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:BooleanToStringConverter x:Key="BooleanToFavoriteIconGlyph" StringIfTrue="&#xF71A;" StringIfFalse="&#xF70F;"/>
    </Page.Resources>

    <ScrollViewer
        x:Name="RootScrollViewer"
        Padding="56,32,56,56">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="NavigationViewExpanded">
                    <VisualState.Setters>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NavigationViewCompact">
                    <VisualState.Setters>
                        <Setter Target="RootScrollViewer.Padding" Value="16,32,16,32"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NavigationViewMinimal">
                    <VisualState.Setters>
                        <Setter Target="RootScrollViewer.Padding" Value="24,32,24,32"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                VerticalAlignment="Center"
                Text="{x:Bind ViewModel.HeaderText, Mode=OneWay}"
                TextTrimming="CharacterEllipsis"
                FontSize="24"/>
            <Border
                Grid.Row="0"
                Grid.Column="1">
                <CommandBar
                    IsOpen="False"
                    DefaultLabelPosition="Right">
                    <AppBarButton
                        Command="{x:Bind ViewModel.ToggleSelectedMenuItemFavoriteCommand}"
                        Visibility="{x:Bind ViewModel.IsSelectedMenuItemSupportFavorite, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Label="{x:Bind ViewModel.GetFavoriteButtonText(ViewModel.IsSelectedMenuItemAFavoriteTool), Mode=OneWay}">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="{x:Bind ViewModel.IsSelectedMenuItemAFavoriteTool, Mode=OneWay, Converter={StaticResource BooleanToFavoriteIconGlyph}}"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <win:AppBarButton
                        Label="{x:Bind strings:MainWindow.PinToStart}">
                        <win:AppBarButton.Icon>
                            <win:FontIcon Glyph="&#xF602;"/>
                        </win:AppBarButton.Icon>
                    </win:AppBarButton>
                </CommandBar>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
